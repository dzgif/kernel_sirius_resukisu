name: Build CrDroid Base + rsuntk KSU

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    
    steps:
      - name: Checkout CrDroid base kernel
        run: |
          git clone --depth=1 --single-branch -b 12.0 \
            https://github.com/MrMiy4mo-xmsirius/uixdess_kernel_xiaomi_sdm710.git kernel
          cd kernel
          # Add KSU from rsuntk's fork
          git clone --depth=1 https://github.com/rsuntk/KernelSU /tmp/ksu
          ln -sf /tmp/ksu/kernel drivers/kernelsu
          # Add to Makefile and Kconfig
          grep -q "kernelsu" drivers/Makefile || echo 'obj-$(CONFIG_KSU) += kernelsu/' >> drivers/Makefile
          grep -q 'source "drivers/kernelsu/Kconfig"' drivers/Kconfig || sed -i '/endmenu/i\source "drivers/kernelsu/Kconfig"' drivers/Kconfig
          echo "=== KSU check ==="
          ls drivers/kernelsu/Kconfig 2>&1
          head -5 drivers/kernelsu/Kconfig

      - name: Setup build environment
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            build-essential bc bison flex libssl-dev libelf-dev \
            cpio kmod gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

      - name: Setup Clang
        run: |
          mkdir -p /tmp/clang && cd /tmp/clang
          wget -q https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-r547379.tar.gz -O clang.tar.gz
          tar xzf clang.tar.gz
          echo "CC=/tmp/clang/bin/clang" >> $GITHUB_ENV
          echo "CLANG_TRIPLE=aarch64-linux-gnu-" >> $GITHUB_ENV

      - name: Build kernel
        run: |
          cd ${{ github.workspace }}/kernel
          
          make ARCH=arm64 mrproper
          make ARCH=arm64 sirius_defconfig
          
          # Enable KSU
          echo "CONFIG_KSU=y" >> .config
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> .config
          
          make ARCH=arm64 olddefconfig
          
          echo "=== KSU config ==="
          grep CONFIG_KSU .config
          
          # Build
          make -j$(nproc) ARCH=arm64 \
            CC=${{ env.CC }} \
            CLANG_TRIPLE=${{ env.CLANG_TRIPLE }} \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            Image

      - name: Package artifacts
        if: always()
        run: |
          mkdir -p ${{ github.workspace }}/output
          find ${{ github.workspace }}/kernel -name "Image" -path "*/boot/*" -exec ls -lh {} \;
          find ${{ github.workspace }}/kernel -name "Image" -path "*/boot/*" -exec cp {} ${{ github.workspace }}/output/ \;
          cp ${{ github.workspace }}/kernel/.config ${{ github.workspace }}/output/kernel-config 2>/dev/null || echo "config not found"
          cd ${{ github.workspace }}/output
          echo "Date: $(date -u)" > build-info.txt
          echo "Source: uixdess_kernel_xiaomi_sdm710 12.0 + rsuntk/KernelSU" >> build-info.txt
          ls -lh

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kernel-crdroid-sirius-${{ github.sha }}
          path: output/
          retention-days: 30
